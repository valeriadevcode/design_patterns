package com.example.p2_arqui.model

class Entrenador(
    id: Int,
    nombre: String,
    telefono: String,
    usuario: String,
    password: String
) : Persona(id, nombre, telefono, usuario, password)
